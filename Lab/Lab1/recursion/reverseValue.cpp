#include <iostream>
using namespace std;
    
int reverse(int n) {
    if(n == 0)
        return 0;
    return (reverse()
}
int main() {
    int n;
    cin >> n;
    cout << reverse(n);
    return 0;
}
